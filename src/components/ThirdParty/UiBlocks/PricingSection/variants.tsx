import { cva } from "class-variance-authority"


export const pricingSectionVariants = cva(
    [],
    {
      variants: {},
      defaultVariants: {},
    }
  )

export default pricingSectionVariants